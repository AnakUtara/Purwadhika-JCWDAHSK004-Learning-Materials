import { Router } from "express";

const tagsResource = Router();

// tagsResource.get("/");
// tagsResource.get("/:id");
// tagsResource.post("/");
// tagsResource.patch("/:id"); // soft delete/restore data
// tagsResource.put("/:id"); // update data
// tagsResource.delete("/:id"); // hard delete

export default tagsResource;
